import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    primaryDark: {
      600: '#yourPrimaryDark600Color',
      // Add other shades if needed
    },
    // ... other palette configurations
  },
  // ... other theme configurations
});

export default Theme;
